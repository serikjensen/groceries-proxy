openapi: 3.1.0
info:
  title: Groceries Write Proxy
  version: 1.0.0
servers:
  - url: https://YOUR-VERCEL-APP.vercel.app
paths:
  /api/update:
    post:
      operationId: updateGroceries
      summary: Update grocery data in Firebase
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                data:
                  type: object
      responses:
        '200':
          description: Success
